# Supabase Postgres (EU region) — pooled connection string for the app,
# direct connection for migrations. Use the "Session" pooler or direct URL
# for drizzle-kit migrate (port 5432), pooled (port 6543) for serverless runtime.
DATABASE_URL=

# Supabase project (client-side integration; safe to expose the anon key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Email (magic links + notifications)
RESEND_API_KEY=

# AI
ANTHROPIC_API_KEY=
